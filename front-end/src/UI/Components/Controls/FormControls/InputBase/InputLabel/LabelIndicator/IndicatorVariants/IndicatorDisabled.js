import React from "react";
import Indicator from "./Indicator";
import Block from "@material-ui/icons/Block";

const IndicatorDisabled = (props) => <Indicator icon={Block} {...props} />;

export default IndicatorDisabled;
